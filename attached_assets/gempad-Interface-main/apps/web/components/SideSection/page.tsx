import React from 'react';

const SideSection = () => {
    return (
        <div style={{ position: 'fixed', left: 0, background: 'red', height: 'inherit' }}>
            <div>SideSection</div>
        </div>
    );
};

export default SideSection;
